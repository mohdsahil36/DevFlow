import { type ChartConfig } from "@/components/ui/chart";

const chartConfig = {
  value: {
    label: "Tasks",
    color: "var(--chart-1)",
  },
} satisfies ChartConfig;

export default chartConfig;
